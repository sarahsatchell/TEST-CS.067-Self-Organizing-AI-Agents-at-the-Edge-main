name: Test and Validate

# Trigger this workflow on pull requests to main branch
on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  test-frontend:
    runs-on: ubuntu-latest

    steps:
      # Checkout the code
      - name: Checkout code
        uses: actions/checkout@v4

      # Setup Node.js environment
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: front-end/package-lock.json

      # Install dependencies
      - name: Install dependencies
        working-directory: ./front-end
        run: npm ci

      # Run linting
      - name: Run ESLint
        working-directory: ./front-end
        run: npm run lint

      # Run tests with coverage
      - name: Run tests with coverage
        working-directory: ./front-end
        run: npm run test:coverage

      # Build the project
      - name: Build project
        working-directory: ./front-end
        run: npm run build

      # Upload coverage reports (optional - for viewing coverage results)
      - name: Upload coverage reports
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: front-end/coverage/

  test-backend:
    runs-on: ubuntu-latest
    needs: test-frontend
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Python dependencies
        working-directory: ./backend
        run: |
          python -m pip install --upgrade pip
          pip install pytest pytest-asyncio

      - name: Run backend tests
        working-directory: ./backend
        run: |
          pytest tests/test_NodeClass.py --maxfail=1 --disable-warnings -q

  # Add more jobs here for back-end testing when needed
  # test-backend:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4
  #     # Add back-end testing steps here
